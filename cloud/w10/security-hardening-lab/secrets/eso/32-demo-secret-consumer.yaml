apiVersion: apps/v1
kind: Deployment
metadata:
  name: secret-consumer
  namespace: flipkart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secret-consumer
  template:
    metadata:
      labels:
        app: secret-consumer
    spec:
      containers:
        - name: app
          image: busybox:1.36
          command: ["/bin/sh", "-c"]
          args:
            - "while true; do date; cat /etc/flipkart-backend-secrets/DB_PASSWORD; sleep 10; done"
          volumeMounts:
            - name: flipkart-backend-secrets
              mountPath: /etc/flipkart-backend-secrets
              readOnly: true
      volumes:
        - name: flipkart-backend-secrets
          secret:
            secretName: flipkart-backend-secrets
