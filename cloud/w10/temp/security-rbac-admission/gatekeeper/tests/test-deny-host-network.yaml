apiVersion: v1
kind: Pod
metadata:
  name: deny-host-network
  namespace: demo
  labels:
    owner: platform-team
spec:
  hostNetwork: true
  containers:
    - name: app
      image: nginx:1.27.0
      resources:
        limits:
          cpu: 100m
          memory: 128Mi

