apiVersion: v1
kind: Pod
metadata:
  name: deny-root-user
  namespace: demo
  labels:
    owner: platform-team
spec:
  containers:
    - name: app
      image: nginx:1.27.0
      securityContext:
        runAsUser: 0
      resources:
        limits:
          cpu: 100m
          memory: 128Mi

