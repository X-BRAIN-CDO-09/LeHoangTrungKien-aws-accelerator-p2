apiVersion: v1
kind: Pod
metadata:
  name: deny-latest
  namespace: demo
  labels:
    owner: platform-team
spec:
  containers:
    - name: app
      image: nginx:latest
      resources:
        limits:
          cpu: 100m
          memory: 128Mi

